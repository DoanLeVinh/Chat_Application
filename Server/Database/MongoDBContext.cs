using MongoDB.Driver;

namespace ChatServer.Database
{
    // TODO: Implement MongoDB connection and context
    public class MongoDBContext
    {
        // Example: GetDatabase(), GetCollection<T>(), etc.
    }
}
